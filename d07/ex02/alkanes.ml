class alkane (n : int) =
  object (self)

  (* TODO *)
    (* val name: string;
    val formula: string;
    method to_string : string = name ^ " (" ^ self#formula ^ ")"
    method equals (other : alkane) : bool = self#formula = other#formula *)
  end
